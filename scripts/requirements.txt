requests>=2.31
yt-dlp[default]>=2025.11   # [default] brings the YouTube challenge solver (with Node.js/Deno)
streamlit>=1.50
imageio-ffmpeg>=0.5   # ffmpeg for making Shorts, when none is installed
faster-whisper>=1.1   # captions on Shorts (speech to text on this PC)
send2trash>=1.8       # disk cleanup moves old folders to the Recycle Bin
# Optional, NVIDIA cards only (~1 GB): captions on the graphics card, in well under a second
#   pip install nvidia-cublas-cu12 "nvidia-cudnn-cu12==9.*"
